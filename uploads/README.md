Drop new role card or background images here (any filename, any format —
PNG/JPG/WEBP are fine). Push/upload them to this folder on GitHub and let
Claude know; it'll pull the repo, look at each image, and move it into the
right spot under `apps/web/public/images/` (role cards, start background,
night background, etc.), updating the code to use it.

This folder is just a drop zone — nothing here is actually used by the app.
